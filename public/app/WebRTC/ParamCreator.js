var createNetworkInterfaceParams = function(type, data) {
	var jsonObj = {};
	if(type == "webSocket") {
		if(data["serverAction"]) {
			
		}
		else {
			console.log("No serverAction specified");
		}
	}
};